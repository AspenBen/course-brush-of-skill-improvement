目前就姑且当作version1.0吧
目前的实现很简单，就是当确认是否继续观看的按钮出现时，鼠标移动到该位置并点击
该程序会监视屏幕，等待确认按钮出现，按下


before you run it, you should install packet it depend on, like pyautogui
you should run follow command:
pip install pyautogui
pip install opencv-python

If you want this program fix in your lapter, you should make a screenshot in your lapter to alter the picture - "button1.png".
